1，先配置settings文件（接口条件）；
2，xiaopapa.py信息采集（具体网址要保密）；
3，运行QRcode.py,采集二维码信息（liantu）；
4,  更改index.html中的ip地址；更改HBuilder中js文件夹中的mui.js中的ip地址；
5，打开F盘中F:\Hbuilder，安卓模拟器\安卓模拟器，启动数据线，联通两端；
6，注册成功以后，在HB的qrcode.html中，修改一下chenggong(0,"f8a589a482421e01208b26dc2661ff3f")；
这样就绑定了自己的玩具，就可以添加别人的玩具了；这样写的话就不用扫那个二维码了，直接给添加了一个toy;
7，运行两个文件banana.py和im_serv.py;在火狐浏览器，输入网址http://192.168.11.31:9527/（自己的ip）;
8,开启redis-server;
9,可以发消息了；在火狐发送消息；先开机，将divice_id输入；（1）然后点击“开始废话按钮”
可以发送“我要给爸爸发消息”或者“北京天气预报”前者指定给好友发消息，后者链接
图灵机器人自动回复；（2）点击“发送语音按钮”，然后在手机app就可以收到消息；
（3）点击“录制消息”按钮，说想对“爸爸”说的话，然后点击“发送语音消息按钮”；
或者：点击第一步消息，开始讲话，说类似于“北京天气预报的话：”，他会自动转接图灵机器人；
或者：直接说：“我想听小猪猪”，他就会实现自动点播音频文件的功能；



#备注：
#登录用户名：zhao,密码123，这里要同时运行（1）数据线文件,F:\Hbuilder，运行：安卓模拟器\安卓模拟器\adb.bat文件；
				       （2）mongo数据库,在cmd中，先切换到f盘，输入mongod,然后在打开另外一个cmd,输入mongo；
				       （3）在另外一个cmd中运行redis-server;
				       （4）运行pycharm中banana项目的banana.py文件和im_serv.py文件；
				       （5）可以打开NoSQLBooter for MongoDB，更直观的看mongo数据库的信息；
				       （6）还要运行HBuilder,MyApp文件；

#打开网页以后，先从芒果数据库中取出玩具id,放入输入框中，点击开机
#运行以后可以在浏览地地址栏输入http://192.168.11.31:9527/，运行程序(自己的ip)
#记得要更改index.html中的ip地址：
#注意：这里是两个进程，两条服务；
    # 所有的websocket在前端只能出现一次，出现第二次就被覆盖了；整个app,只能有一个。这是一个坑；
    # 在Myapp 的HBuilder文件中的index.html文件中也有一个mui.plus的地方，需要改ip地址；
    # 在 QRcode.py文件中运行一下，采集到二维码设备信息（就是获取到二维码，相当于生产了设备）